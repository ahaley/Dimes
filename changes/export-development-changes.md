I want to consider the best practices for prompting claude code and how we craft the instructions in the in-development export.

Each change request should be executed on its own branch and committed separately, and then finalized by merging into the calling branch. How should we craft this instruction?