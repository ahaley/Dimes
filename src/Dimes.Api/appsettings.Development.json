{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Dev-only: local auth with a seeded admin so a fresh checkout has an immediate way in.
  // Do NOT use these values in production — set Auth:SiteAdmin via env/Secrets there instead.
  "Auth": {
    "Mode": "Local",
    "SiteAdmin": {
      "Email": "admin@dimes.local",
      "InitialPassword": "dimes-dev"
    }
  }
}
